<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wenbo.myapplication"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="26" />

    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <meta-data
        android:name="android.support.VERSION"
        android:value="26.0.0-alpha1" />

    <application
        android:configChanges="keyboardHidden|orientation"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >
        <activity
            android:name="com.example.product.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- All Product Activity -->
        <activity
            android:name="com.example.product.ViewActivity"
            android:label="教师信息" >
        </activity>

        <!-- Add Product Activity -->
        <activity
            android:name="com.example.product.RegisterActivity"
            android:label="注册" >
        </activity>

        <!-- Edit Product Activity -->
        <activity
            android:name="com.example.product.ViewDetailActivity"
            android:label="Edit Product" >
        </activity>

        <!-- Login Activity -->
        <activity
            android:name="com.example.product.LoginActivity"
            android:label="登陆" >
        </activity>

        <!-- Already Login Activity -->
        <activity
            android:name="com.example.product.LoginActivity2"
            android:label="账号信息" >
        </activity>

        <provider
            android:name="com.android.tools.fd.runtime.InstantRunContentProvider"
            android:authorities="com.example.wenbo.myapplication.com.android.tools.fd.runtime.InstantRunContentProvider"
            android:multiprocess="true" />
    </application>

</manifest>